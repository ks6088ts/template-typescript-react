# Application Insights connection string.
# When set, telemetry (page views, route transitions, exceptions, custom events) is sent to Azure Monitor.
# When empty or unset, telemetry is fully disabled (no-op) — no network requests are made.
# Format: "InstrumentationKey=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx;IngestionEndpoint=https://...;"
VITE_APPLICATIONINSIGHTS_CONNECTION_STRING=
